Arrays.copyOfRange(T[ ] original,int from,int to)//将一个原始的数组original，从下标from开始复制，复制到上标to，生成一个新的数组。包括下标from，不包括上标to。




Collections.reverse(temp);     //倒转集合的元素
Collections.sort(temp,new Comparator());         //集合元素排序